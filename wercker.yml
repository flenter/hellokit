box: wercker/android
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - script:
        name: run gradle connectedInstrumentTest
        code: |
          gradle build

